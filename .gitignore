# Obsidian plugin build artifacts
node_modules/
main.js
*.js.map
data.json

# Uru runtime (derived, rebuildable — never commit)
.uru-data/
index-state.json

# OS
.DS_Store

# Release build output
dist/

# Local Python dev/test artifacts (sidecar/khora). The runtime venv + pinned
# wheel live in per-user app-data; these are only produced by ad-hoc local runs.
.venv/
.coverage
.pytest_cache/
__pycache__/
*.pyc
