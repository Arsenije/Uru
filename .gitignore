# Obsidian plugin build artifacts
node_modules/
main.js
*.js.map
data.json

# Uru runtime (derived, rebuildable — never commit)
.uru-data/
index-state.json

# OS
.DS_Store

# Release build output
dist/

# Generated test fixtures (Wikipedia dump extracts, etc. — never commit)
testfiles/
enwiki_namespace_0_0.jsonl

# Test runner bundle output
.test-build/
